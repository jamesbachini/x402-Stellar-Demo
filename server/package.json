{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "build:paywall": "esbuild paywall-client.js --bundle --format=esm --platform=browser --minify --outfile=public/paywall-client.js",
    "start": "pnpm run build:paywall && node index.js",
    "test": "node --test smoke.test.js"
  },
  "dependencies": {
    "@stellar/freighter-api": "^6.0.1",
    "@stellar/stellar-sdk": "^14.6.0",
    "@x402/core": "2.6.0",
    "@x402/express": "2.6.0",
    "@x402/stellar": "2.6.0",
    "dotenv": "^17.3.1",
    "esbuild": "^0.25.10",
    "express": "^5.1.0"
  }
}
